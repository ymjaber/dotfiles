auto_sync = false           # local-first; sync question reopens with the servers use case
search_mode = "fuzzy"
filter_mode_shell_up_key_binding = "session"
enter_accept = false        # first enter edits the picked command, second enter runs it

